{# Targeted partial swapped into #inspector (not the graph). #}
<h2 style="margin-top:0">{{ node.data.title }}</h2>
<dl>
  <dt>ID</dt><dd>{{ node.id }}</dd>
  <dt>Status</dt><dd>{{ node.data.status }}</dd>
  <dt>Amount</dt><dd>{{ node.data.amount }}</dd>
  <dt>Position</dt><dd>{{ node.x }}, {{ node.y }}</dd>
</dl>
