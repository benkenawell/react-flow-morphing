{# Response for an inspector edit: the whole graph (morphed into #graph) plus a
   partial that refreshes #inspector so the panel reflects the saved server state. #}
{% include "_graph.njk" %}
<hx-partial hx-target="#inspector" hx-swap="innerHTML">
{% include "_inspector.njk" %}
</hx-partial>
